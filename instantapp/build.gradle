apply plugin: 'com.android.instantapp'

// Run './gradlew instantApp:assembleRelease' to build the instant app zip

dependencies {
    implementation project(':main')
    implementation project(':base')
}
